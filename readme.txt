step1. create a database named 'ewsd' and import 'ewsd.sql' file

step2. create a file named with '.env' same with path with '.env.example' file

step3. copy all code from 'database-code.txt' to '.env' file

step4. install composer to import 'vendor' folder in porject

step5. please key generate with artisan using "php artisan key:generate"

step6. Start the project to run with "php artisan serve"

